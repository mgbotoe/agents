# 2026-05-09

## WDAI PR #598 review (Brigitte's cohort registration)

**PR:** feat(events+courses): replace Luma cohort events with one-click Mailchimp registration. Brigitte Lyons, +560/-348, 15 files. CI green, Vercel preview blocked on commit-author email mismatch (her access issue, not Brigitte's to fix).

**What it does:** Replaces visible Mailchimp embed form on /courses with one-click POST to `/api/cohort/register`. Route reads name/email from Clerk session, POSTs to Mailchimp embed action URL. Adds `CohortEventCard` injected into /events monthly feed. Updates CLAUDE.md rule #1 to describe new flow.

**Review process:** Read CLAUDE.md, route.ts, CohortEventCard, EventsClient, CoursesClient, types.ts, rsvp-actions.ts. Verified `submitCohortRsvp` is upsert keyed on (userId, courseId, cohortDate). Verified `cohortDate` is human-readable string ("May 4, 2026") not ISO. Confirmed `MailchimpForm.tsx` deleted. Ran typecheck (clean), vitest (236/236 pass), biome (4 errors but all CRLF artifacts from Windows checkout — CI clean). PR adds 0 tests.

**Pressure tested via advisor before posting** — initial review had M1 (hydration risk) which I retracted after verifying cohortDate is human-readable (V8 parses unanchored strings as local-TZ on both server and client → no mismatch). H3 (rate limit) downgraded to MEDIUM. H1 sharpened to architectural framing. Skipped Vercel-blocker note per Dina (it's an access issue, not Brigitte's problem). Removed Helen UX speculation (was projecting), tagged Helen at the end of the comment to ask what she meant.

**Comment posted:** https://github.com/WomenDefiningAI/wdai-foundation-platform/pull/598#issuecomment-4413830983

Findings (suggestions, not blockers):
1. **Must-fix:** /events and /courses paths asymmetric — only /courses writes DB record + emits PostHog. Offered B-or-C: server-side DB write (cleaner, fixes existing fire-and-forget durability bug too) or mirror-pattern in CohortEventCard (smaller diff). Dina's call which to recommend; offered both.
2. **Must-fix:** `route.ts:37-41` doesn't check Mailchimp response.ok, bare catch swallows errors. Should log status + body excerpt to Sentry (#583 just wired).
3. **Should-fix:** Honeypot list-id hardcoded — move to courses.ts or env.
4. **Should-fix:** CohortEventCard has no userHasRsvp plumbing.

**Scalability lens (not in posted comment):** at WDAI scale (hundreds of members, 3 cohorts/year), fine. Real ceiling is Mailchimp embed URL as backbone — not a documented API, no retry/idempotency, brittle at burst. Move to Mailchimp Marketing API + queue when it matters. Not this PR.

## Session feedback for memory

- **Don't speculate on what reviewers mean** — when Helen said "a detail about how we show the event w/out a luma block," I assumed UX. Dina caught it. Better to ask the reviewer directly than project.
- **Vercel preview blockers from contributor commit-email aren't reviewer-actionable** — that's Helen's access-grant problem, not Brigitte's commit to fix. Don't lead with procedural blockers contributors can't resolve.
- **Pressure-test recommendations before posting.** Steelmanned alternative architectures (B vs C in finding #1) and offered both rather than dictating one. Better for collaborative repos with non-developer contributors.
- **Advisor caught two retractions:** M1 hydration was wrong (cohortDate is human-readable not ISO), H3 was overstated. Both before posting. Worth doing this on every external-repo review.
