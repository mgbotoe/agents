# Daily Log — 2026-05-11

## [15:57] Session Distill

### Accomplished
- **WDAI intro-matcher PR #603 shipped end-to-end.** Replaces stale Gumloop+Airtable matchmaker with a WDAI HTTP endpoint that returns fresh DB candidates; Gumloop keeps trigger/LLM/posting. Final state: 8 files, 789-line net diff, 270/270 tests, ready for review.
- **Iterated through 4 architectures** before landing the right one: v1 inline portal hook (coverage regression) → v2 in-house cron (Vercel cron preview-only blocker) → v3 endpoint with WDAI calling Anthropic → **Path B endpoint, Gumloop owns LLM** (the right answer).
- **Smoke-tested end-to-end** against staging DB: auth, JSON parse, Slack→user lookup, CAS, candidate selection, sanitized output all verified. Returned 17 real candidates from test data.
- **PR body slimmed iteratively** in response to Dina's feedback. Final form: Summary → inline Setup (secret gen, env, Gumloop wiring) → Test plan → Rollback → Future add-ons. Setup doc deleted (over-explained standard ops).
- **Three secrets exposed in chat** during testing (Anthropic key, two `MATCHER_API_SECRET` rotations, Vercel bypass). Flagged to rotate after Gumloop cutover.

### Open / unfinished
- Vercel preview + prod env vars NOT yet set (`MATCHER_API_SECRET`, `INTRO_MATCHER_ENABLED=true`)
- Gumloop duplicate flow NOT yet wired (operator task per PR's Setup section 3)
- CI status on latest commit not verified

### Decisions worth ADR'ing later
- **Gumloop stays as trigger + LLM + poster**; WDAI is data-only. Trade: third-party dep retained, but matching brain + Anthropic billing stay where they already worked.
- **No Anthropic in WDAI**: dropped `@anthropic-ai/sdk`, dropped Sentry F4 opt-out, dropped `findMatches`/`runIntroMatcher`. WDAI's role is "smarter Airtable view," not "AI service."
- **CAS via `slackMatchPostedAt` + `matchingOptOut` column kept** despite YAGNI pressure, on the assumption follow-up PRs (opt-out UI, backfill cron) will use them.

### Patterns Dina demonstrated
- Pushes back on PR scope size, citing WDAI's lean-PR culture (typical 1-5 files vs my 12). Real test isn't lines but **"how many separable concerns surface"** — bundled architectural artifacts inflate cognitive load even when net code is small.
- Repeatedly questions docs/plans bloat. The 968-line QA test-plan artifact survived 4 pivots before she caught it. Lesson: when a plan is superseded by working code, delete the plan in the same commit.
- Prefers in-PR setup detail over linked-doc when the linked-doc is mostly standard ops repetition. "We still need to know how to generate the secret" — keeps PR self-contained for things specifically introduced by the PR.
- "Naw let just ship as is" / "ok let see the new content" — terminate iteration cleanly. Wants explicit checkpoint before pushing substantive PR edits.
