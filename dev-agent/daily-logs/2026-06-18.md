# 2026-06-18

## Snapshot 17:56 UTC (SessionEnd)
- 3 external files touched: settings.json, 003-compound-engineering-adoption.md, CLAUDE.md

## Snapshot 18:03 UTC (SessionEnd)
- 3 external files touched: settings.json, 003-compound-engineering-adoption.md, CLAUDE.md
- Session duration: ~6m


## Commits
- 18:06 UTC [agents] `b223f6e` chore(daily-logs): session end 2026-06-18 17:56 UTC [+0/-0, 0 files]
- 18:24 UTC [agents] `c0c98a2` chore(cross-platform): path portability + port heartbeat watchdog to Python [+371/-13, 9 files]
- 18:24 UTC [agents] `a7e01f3` chore(cross-platform): move recurring automation off Windows Task Scheduler [+416/-78, 7 files]
- 18:24 UTC [agents] `c91ba7e` docs(cross-platform): rewrite scheduling docs to cloud cron + hooks + /loop [+21/-19, 2 files]

- 18:40 UTC [agents] `a7a61c1` chore(mac-port): wire cross-machine skill-sync + workstation map; drop dead ps1 [+178/-139, 3 files]
- 18:49 UTC [agents] `7c1922e` Revert "docs(cross-platform): rewrite scheduling docs to cloud cron + hooks + /loop" [+19/-21, 2 files]
- 18:52 UTC [agents] `f0ff7e7` chore(wiki): commit orphaned 2026-05-12 WDAI core team sync source [+102/-0, 1 files]
- 18:53 UTC [agents] `d21d079` refactor(cross-platform): per-machine workspace map via shared loader [+97/-23, 5 files]
- 18:56 UTC [agents] `8bf6b06` fix(cross-platform): OS-neutral python interpreter in dev-agent hooks [+16/-16, 2 files]
- 18:59 UTC [agents] `c9b0c08` chore(daily-logs): session end 2026-06-18 18:57 UTC [+23/-1, 1 files]
- 19:00 UTC [agents] `471ffdf` feat(advisor): existing-decision scan + /advisor skill [+97/-1, 2 files]
- 19:06 UTC [agents] `b9dcb46` feat(cross-platform): path-leak CI guard (Bucket 1.5) [+172/-4, 4 files]
- 21:54 UTC [agents] `b618ab7` security(atlas): fail-closed gate on Tier-2 daily-drivers (CRITICAL injection finding) [+36/-0, 6 files]
## Self-Modification — cross-platform (Mac + Windows)

**What:** Made Polaris OS-agnostic for sequential use across a living-room Windows box and an upstairs Mac. Branch `chore/cross-platform-agnostic` (3 commits, not yet merged).
- Path derivation via `parents[N]` (no hardcoded `C:\`); `.mcp.json.example` + `config/secrets.env.example` templates; `bin/setup-mac.sh`; `.gitattributes` eol rules.
- Ported `gather-context.ps1` → `gather-context.py` (cross-platform; fixed 0GB disk bug via `shutil.disk_usage`; safe ctypes/os.kill liveness probe — `os.kill(pid,0)` would TerminateProcess on Windows).
- Heartbeat → `/loop` (local-state watchdog, no cloud home). Decay + self-improve → cloud cron (`decay.yml`, `self-improve.yml`). Self-improve is **PR-gated** (opens PR, never writes master; security.md/SOUL.md/CLAUDE.md off-limits to auto-edit). Deleted all Windows Task Scheduler `.cmd`/`.bat` + `.ps1`; `bin/scheduled/` gone.

**Why:** Windows Task Scheduler is single-OS and was already dead (no `\Polaris\` tasks since the 2026-05-07 cloud-cron migration). Cloud cron + hooks + `/loop` are all OS-agnostic. Advisor (fork) corrected 3 premises: heartbeat needs local-machine visibility (no cloud home — use `/loop`); deleting `run-task.cmd` would silently kill `/self-improve` (gave it a cloud home first); SessionStart-folding would drop Atlas-inbox polling (preserved it).

**Issues:** (1) Hit a corrupt git index mid-session (`bad signature 0x00000000`, interrupted write) — recovered via backup-aside + `git reset` (history/working-tree intact). (2) Advisor's `os.kill(pid,0)` liveness suggestion is a Windows footgun — diverged to ctypes `OpenProcess`. (3) self-improve.yml's PR step needs repo setting "Allow GitHub Actions to create and approve pull requests" enabled. (4) Cloud jobs are compile-checked + logic-verified but unverified in-cloud until first run / `workflow_dispatch`.

## [11:56] Session Distill

**Done:**
- Extracted `custom-skills` → new **private `mgbotoe/claude-skills`** marketplace repo (17 skills). Re-scoped `~/.claude` → **`mgbotoe/my.claude`** (683→29 tracked): gitignored vendored `plugins/`, split machine-local hooks to `settings.local.json`, declared all 6 marketplaces in `settings.json`.
- Built **`sync-skills.py`** SessionStart hook (self-healing): Claude Code doesn't auto-pull github marketplaces (issue #44276), so it git-pulls the marketplace clone each session on every machine. Wired Windows + `setup-mac.sh` (python shim, gh-auth, idempotent reg). One-time Windows cache swap done + verified.
- `docs/workstation-map.md`: Windows→Mac config port inventory (port/clean per item).
- Diagnosed **Atlas meeting-prep pipeline DEAD since 2026-05-12** (Task Scheduler killed in 5-07 cloud migration, `.ps1` never moved to cloud; missed WDAI Core Team Syncs May 19/Jun 2, Helen sessions) → posted `#atlas-cos`, committed orphaned May 12 source (`f0ff7e7`).

**Open:** verify `settings.local.json` hooks merge next session (`[sync-skills] up to date`); then delete `custom-skills.bak-directory-source`; `f0ff7e7` unpushed; deferred: Atlas `.ps1` port (meeting-prep fix), CE-overlap consolidation, wire `/advisor`→council.

**Decision (ADR candidate):** cross-machine config = 3 repos (my.claude / claude-skills / agents) + declarative `extraKnownMarketplaces` + per-machine hooks in `settings.local.json` (no portable config-dir token, issue #4276) + marketplace freshness via SessionStart git-pull hook.

**Dina patterns:** demands verification not assertion (caught `advisor()` is unwired prose, not a real tool); step-gates every push even after plan approval; cleanup-as-you-go ("opportunity to clean things"); wants a workstation map to discuss before executing.

## Snapshot 18:57 UTC (SessionEnd)
- 6 external files touched: settings.json, settings.local.json, .gitignore, .gitattributes, .gitattributes, sync-skills.py
- Delegation: soft-warn fired (4+ external files)

## Self-Modification — cross-platform rework (autonomous /loop)

**What:** Reworked branch `chore/cross-platform-agnostic` to align with the pre-existing port plan + ADR (which I'd missed on the first pass). 7 commits, each pushed to the branch. Loop ran while Dina was away; branch-only, no master push, no PR merge.
- `7c1922e` revert docs/memory commit (cleared the PR's memory.md conflict non-destructively; handed CLAUDE.md/memory to Atlas's lane).
- `d21d079` workspace map → shared `_workspace.py` + gitignored `workspace.local.json` (+ .example); de-hardcoded workspace-scan.py AND log-commit.py (Bucket 1.2/1.3).
- `8bf6b06` python gap: Atlas's `python3||python||true` for fire-and-forget hooks; single-run `PY=$(...)` for the exit-2 blocking repo-aware gate + context-injector (preserves exit code, no double-run, symlink-independent).
- `471ffdf` the REAL fix: `context-injector` now scans `wiki/decisions/` (it only scanned `wiki/projects/` — why the cross-platform ADR never surfaced) + `/advisor` skill (fork-based).
- `b9dcb46` `bin/scan-paths.py` + `port-guard.yml` CI (Bucket 1.5 path-leak guard).

**Why/decisions:** Advisor (fork) pressure-tested the enforcement design → PROCEED-WITH-CHANGES: dropped the L2 PreToolUse block (wrong triggers, would've missed today's low-keyword pivots), kept L1 as an honest heuristic nudge, and added the existing-decision scan as the actual root-cause fix. SDK baseline confirmed hooks are the native enforcement primitive (not hand-rolling). `/advisor` built as a **fork** skill, NOT wired to council (deferred note said council) — fork is cross-platform with zero per-machine CLI deps, which fits the Mac+Windows goal better; flag for Dina.

**Issues:** (1) `scan-paths.py` initially swallowed relative-path findings via a broad except → would have passed leaky files in CI; caught in testing, fixed. (2) Drive-letter regex false-positived on `word:\n` escape sequences → standalone-drive lookbehind. (3) Did NOT merge origin/master into the branch — the cross-platform ADR isn't in the branch tree yet, so the decision-scan can't surface it until merge (validated against a local decision file instead). (4) daily-logs on a feature branch will conflict with master's copy — left uncommitted; needs handling at merge/session-end.